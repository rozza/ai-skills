# jira-work Skill

## Workspace Convention

Store all eval/benchmark workspace output in the `workspace/` directory within this
skill directory (i.e., `jira-work/workspace/`), not in a sibling `jira-work-workspace/`
directory.
